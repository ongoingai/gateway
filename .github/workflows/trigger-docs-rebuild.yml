name: Trigger docs site rebuild

on:
  push:
    branches: [main]
    paths: [docs/**]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
